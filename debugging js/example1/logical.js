const numbers = [1, 2, 3, 4];

if (numbers.length = 4) {
  console.log('success');
}
else {
  throw 'array is not the correct size';
}

function printDescending(number) {
  while (number >= 0) {
    console.log(number);
  }
}